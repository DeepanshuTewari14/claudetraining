# claudetraining

Self-contained mockup of an internal IT Support Service Desk for a fictional "UOB Bank" training/demo. Everything — markup, CSS, and JavaScript — lives in a single `index.html` file with no build step or dependencies.

![Screenshot](assets/screenshot.png)

Open `index.html` directly in a browser to view it — no server required.
